# ============================================================================
# 本地开发环境变量示例
# 
# 使用方法：
# 1. 复制此文件为 .dev.vars
#    cp .dev.vars.example .dev.vars    (Linux/Mac)
#    copy .dev.vars.example .dev.vars  (Windows)
# 2. 填入你的实际配置值
# 3. 运行 npm run dev 启动本地开发服务器
#
# 注意：.dev.vars 文件已被 .gitignore 忽略，不会上传到 Git
# ============================================================================

# ============================================================================
# 开发模式配置（重要！）
# ============================================================================

# 开发模式开关
# 设置为 "true" 启用开发模式，默认 UUID 将生效
# 生产环境不要设置此变量（或设置为空）
DEV_MODE=true

# ============================================================================
# 安全配置
# ============================================================================

# API 访问密钥（必填）
# 保护 /api/* 端点，防止敏感信息泄露
# 不设置此变量，所有 API 端点将返回 401 错误
API_KEY=your-api-key-here

# ============================================================================
# 必填配置（仅开发模式生效）
# ============================================================================

# 默认 UUID（开发模式必填）
# 用于 VLESS 客户端验证
# 生成新 UUID: https://www.uuidgenerator.net/
# 注意：仅当 DEV_MODE=true 时，此 UUID 才会生效
UUID=d342d11e-d424-4583-b36e-524ab1f0afa4

# ============================================================================
# 可选配置
# ============================================================================

# 代理 IP（可选）
# TCP 连接失败时的备用代理地址
# PROXY_IP=cdn.example.com

# DNS 服务器（可选）
# 默认使用 Cloudflare DNS
# DNS_SERVER=https://1.1.1.1/dns-query

# ============================================================================
# Remnawave API 配置（可选）
# 如果不使用 Remnawave，保持注释状态即可
# 无论开发模式还是生产模式，Remnawave 获取的 UUID 都会生效
# ============================================================================

# Remnawave 面板 API 地址
# RW_API_URL=https://your-panel.example.com

# Remnawave API 密钥
# RW_API_KEY=your-api-key-here

# UUID 缓存时间（秒），默认 300（5分钟）
# UUID_CACHE_TTL=300
